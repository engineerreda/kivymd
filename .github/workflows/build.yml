name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up Docker
      - name: Set up Docker
        run: |
          sudo apt-get update
          sudo apt-get install -y docker.io
          sudo systemctl start docker

      # Step 3: Pull official Buildozer Docker image
      - name: Pull Buildozer Docker Image
        run: |
          docker pull kivy/buildozer

      # Step 4: Initialize buildozer.spec if missing
      - name: Initialize buildozer.spec if missing
        run: |
          if [ ! -f "buildozer.spec" ]; then
            echo "Creating a new buildozer.spec file..."
            buildozer init
          fi

      # Step 5: Build APK using Docker
      - name: Build APK using Docker
        run: |
          docker run --rm \
            -v ${PWD}:/project \
            -w /project \
            -e ANDROID_API=30 \
            -e ANDROID_MINAPI=21 \
            -e ANDROID_NDK_VERSION=25b \
            kivy/buildozer android debug

      # Step 6: Upload artifacts
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: bin/*.apk
